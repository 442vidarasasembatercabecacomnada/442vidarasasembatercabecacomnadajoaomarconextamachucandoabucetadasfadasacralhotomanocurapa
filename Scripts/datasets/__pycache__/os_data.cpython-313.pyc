--//---------------------------------------------------------------------------------------------------------------
-- Binds

return function(Tab, Fluent)

    do

        Tab:AddSection("Main")

        Tab:AddKeybind("parrybind", {
            Title = "Parry bind",
            Mode = "Toggle",
            Default = "...",
            Callback = function(Value)
                _G.AutoParryToggle:SetValue(not _G.AutoParryToggle.Value)
            end
        })

        Tab:AddKeybind("abilitybind", {
            Title = "Auto ability bind",
            Mode = "Toggle",
            Default = "...",
            Callback = function(Value)
                _G.AutoAbilityToggle:SetValue(not _G.AutoAbilityToggle.Value)
            end
        })

        Tab:AddSection("Farm")

        Tab:AddKeybind("autofarmbind", {
            Title = "Auto farm bind",
            Mode = "Toggle",
            Default = "...",
            Callback = function(Value)
                _G.autoFarmToggleObject:SetValue(not _G.autoFarmToggleObject.Value)
            end
        })

        Tab:AddSection("Rage")

        Tab:AddKeybind("InfparryBind", {
            Title = "Infinite parry bind",
            Mode = "Toggle",
            Default = "...",
            Callback = function(Value)
                _G.InfParryToggle:SetValue(not _G.InfParryToggle.Value)
            end
        })

        Tab:AddKeybind("GodBind", {
            Title = "God mode bind",
            Mode = "Toggle",
            Default = "...",
            Callback = function(Value)
                _G.GodToggle:SetValue(not _G.GodToggle.Value)
            end
        })

        Tab:AddKeybind("FlyBind", {
            Title = "Invisible bind",
            Mode = "Toggle",
            Default = "...",
            Callback = function(Value)
                _G.FlyToggle:SetValue(not _G.FlyToggle.Value)
            end
        })

        Tab:AddKeybind("NoclipBind", {
            Title = "Noclip bind",
            Mode = "Toggle",
            Default = "...",
            Callback = function(Value)
                _G.NoclipToggle:SetValue(not _G.NoclipToggle.Value)
            end
        })

        Tab:AddKeybind("InfDashBind", {
            Title = "Inf dash bind",
            Mode = "Toggle",
            Default = "...",
            Callback = function(Value)
                _G.InfDashToggle:SetValue(not _G.InfDashToggle.Value)
            end
        })

        Tab:AddKeybind("DashCooldwnBind", {
            Title = "Dash cooldwn bind",
            Mode = "Toggle",
            Default = "...",
            Callback = function(Value)
                _G.NoDashCoolToggle:SetValue(not _G.NoDashCoolToggle.Value)
            end
        })

        Tab:AddKeybind("InfJumpBind", {
            Title = "Inf jump bind",
            Mode = "Toggle",
            Default = "...",
            Callback = function(Value)
                _G.InfJumpToggle:SetValue(not _G.InfJumpToggle.Value)
            end
        })

    end
    
end
